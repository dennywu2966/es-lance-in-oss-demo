## Codebase Patterns

- **Python script pattern in Node.js**: Use heredoc syntax (`python3 - <<'PYEOF'\\n${script}\\nPYEOF`) to execute Python code from TypeScript files. Remember to clear proxy environment variables before importing Python libraries.
- **OSS credentials management**: OSS credentials are read from environment variables (OSS_ACCESS_KEY_ID, OSS_ACCESS_KEY_SECRET, OSS_BUCKET, OSS_REGION). Never hardcode credentials in frontend code.
- **API route structure**: All API routes use `app/api/[resource]/[action]/route.ts` pattern with POST handlers that return JSON responses with `{ success: boolean, ... }` structure.
- **Error handling**: Always wrap Python execution in try-catch blocks with cleanup of temp directories using `fs.rm(tempDir, { recursive: true, force: true })`.
- **Elasticsearch integration**: ES uses Basic auth (base64-encoded credentials), bulk API expects NDJSON format (newline-delimited JSON), dense_vector field requires exact dimensions, cosine similarity is default metric.
- **Lance dataset operations**: Extract vectors with `.as_py().tolist()` pattern, use `dataset.to_table(columns=[...])` for selective loading, always cleanup temp directories after operations.

## 2025-01-27 12:40 - US-001
- Implemented document generation with Jina embeddings (jina-embeddings-v2-base-en model, 768 dimensions)
- Created 20 hardcoded fake documents covering tech topics: AI, databases, search engines, cloud computing, ML, distributed systems, DevOps, data engineering, software architecture, software development
- Added `generateDocumentsWithEmbeddings()` function in `lib/oss-client.ts` that:
  - Calls Jina API for each document text sequentially
  - Generates Lance dataset with schema: id, title, text, topic, created_at, vector
  - Creates IVF_PQ vector index with num_partitions = max(2, len // 5) and num_sub_vectors = min(dims // 8, 64)
  - Uploads to OSS at `lance-documents/dataset.lance`
  - Returns success status, document count, and OSS path
- Created API route `app/api/vectors/documents/generate/route.ts` (POST endpoint)
- Updated `.env.example` to include JINA_API_KEY

**Files changed:**
- `lib/oss-client.ts`: Added FakeDocument interface, FAKE_DOCUMENTS constant (20 docs), generateDocumentsWithEmbeddings() function
- `app/api/vectors/documents/generate/route.ts`: New API route
- `.env.example`: Added JINA_API_KEY configuration

**Learnings for future iterations:**
- The Jina API is called sequentially (20 requests). For production, consider batching or parallel requests with rate limiting.
- Lance dataset schema uses `pa.string()` for Java compatibility (avoid `pa.utf8()`).
- Python script must clear proxy environment variables (`http_proxy`, `https_proxy`, etc.) before importing libraries to avoid import errors.
- IVF_PQ index parameters are calculated based on dataset size: num_partitions = max(2, min(n // 5, 8)) for small datasets (20 docs = 4 partitions).
- Error handling tracks failed documents but continues processing others.
- Temp directories use `/tmp/lance-docs-${Date.now()}` pattern to avoid conflicts.
---

## 2025-01-27 - US-002
- Implemented document viewing functionality from Lance dataset stored in OSS
- Created API route `app/api/vectors/documents/route.ts` that:
  - Downloads Lance dataset from OSS (`lance-documents/dataset.lance`)
  - Uses Python to read documents with columns: id, title, text, topic, created_at
  - Returns documents as JSON with total count
  - Cleans up temp directories after processing
- Updated `components/vector-management.tsx` with:
  - New "DOCUMENTS" button (emerald color) in dataset actions
  - Document display panel showing all document fields
  - Loading state with spinning refresh icon during fetch
  - Documents displayed in scrollable container with max-height: 800px
  - Each document shows: index badge, title, topic tag, created date, id, and full text
  - Consistent glass-morphism styling matching existing UI

**Files changed:**
- `app/api/vectors/documents/route.ts`: New API route (POST endpoint)
- `components/vector-management.tsx`: Added Document interface, documents state, loadDocuments function, DOCUMENTS button, and documents display section

**Learnings for future iterations:**
- Lance dataset reading pattern: download from OSS to /tmp, use Python to read with `lance.dataset()`, use `to_table()` with column filters to load specific fields, then cleanup
- Documents are read with `limit` parameter (default 20) to avoid loading massive datasets
- Python script must clear proxy environment variables before importing lance library
- Document display uses similar UI pattern to sampled vectors but with different layout (header with metadata, body with text content)
- Text content is displayed in a smaller font (text-sm) with leading-relaxed for better readability
- Document cards have hover effect: hover:border-emerald-500/30 transition-colors
---

## 2025-01-27 - US-003
- Implemented Elasticsearch backfill functionality for Lance documents
- Updated `app/api/vectors/backfill/route.ts` to:
  - Read documents with embeddings from Lance dataset in OSS (lance-documents/dataset.lance)
  - Create Elasticsearch index with proper mapping for dense_vector field (768 dims, cosine similarity)
  - Index documents in batches (20 docs per batch) using ES bulk API
  - Return detailed response: indexed count, total count, vector dimensions, duration
  - Support optional index creation (default: true)
- Updated `components/vector-management.tsx` to:
  - Add `isBackfilling` state variable
  - Add `backfillToElasticsearch()` function to call the backfill API
  - Add Upload icon import from lucide-react
  - Add "BACKFILL ES" button (blue color) between DOCUMENTS and DELETE buttons
  - Button shows loading spinner during backfill operation
  - Button disabled during other operations (generating, sampling, loading documents)

**Files changed:**
- `app/api/vectors/backfill/route.ts`: Complete rewrite for document backfill (was for old vector datasets)
- `components/vector-management.tsx`: Added backfill state, function, and button

**Learnings for future iterations:**
- Elasticsearch dense_vector field requires exact dimensions specification (768 for Jina embeddings)
- ES bulk API expects newline-delimited JSON (NDJSON) format, not regular JSON array
- ES index creation uses 400 response when index already exists - this is expected behavior
- Lance dataset vector extraction: use `.as_py()` method then `.tolist()` to convert PyArrow data to Python list
- Backfill pattern: download from OSS → read with Python → index to ES → cleanup temp directory
- Batch size of 20 documents keeps bulk requests under ES recommended 10MB limit
- ES authentication uses Basic auth with base64-encoded credentials
- Document mapping includes keyword subfields for exact matching (id, title.keyword)
- Cosine similarity is the default for dense_vector - matches Lance embedding similarity metric
---
